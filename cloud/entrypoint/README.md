# Entrypoint - AWS Lambda Function Code

This sub-directory within the Mobile Observation Ad Toolkit is responsible for holding Amazon Web Services executable Lambda function code, to be loaded into a Python runtime.

The Lambda function code facilitates data collection directly from participants of data donation advertising research studies, as well as handles participant registration data.