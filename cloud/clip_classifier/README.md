# CLIP Classifier - AWS Lambda Function Code

This sub-directory within the Mobile Observation Ad Toolkit is responsible for holding Amazon Web Services executable Lambda function code, to be loaded into a Python runtime.

The Lambda function code involves running a CLIP ML classification over advertisement data, in order to auto-tag the advertisement into a taxonomy of advertisement categories.