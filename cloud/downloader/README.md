# Downloader - AWS Lambda Function Code

This sub-directory within the Mobile Observation Ad Toolkit is responsible for holding Amazon Web Services executable Lambda function code, to be loaded into a Python runtime.

The Lambda function code is responsible for assisting the download of resources from Commercial Content Libraries, for the purpose of enriching advertisement data.